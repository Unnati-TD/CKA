apiVersion: storage.k8s.io/v1
kind: StorageClass
metadata:
  name: standard                # Replace with your StorageClass name
provisioner: kubernetes.io/no-provisioner  # Replace with actual provisioner if dynamic
volumeBindingMode: WaitForFirstConsumer
