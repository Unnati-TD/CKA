apiVersion: v1
kind: Pod
metadata:
  name: pod-name               # Replace with desired Pod name
spec:
  containers:
    - name: container-name     # Replace with container name
      image: image-name        # Replace with container image (e.g., nginx:latest)
      ports:
        - containerPort: 80    # Replace with container port if needed
