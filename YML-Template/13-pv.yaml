apiVersion: v1
kind: PersistentVolume
metadata:
  name: pv-name                 # Replace with PV name
spec:
  capacity:
    storage: 1Gi                # Replace with desired capacity
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: /mnt/data             # Replace with actual path on host
